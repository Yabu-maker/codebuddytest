# 2026-06-02

- 按用户要求再次将 `/Applications/CodeBuddy CN.app/Contents/Resources/app/extensions/genie/product.json` 中的 `endpoint` 从 `https://copilot.tencent.com` 修改为 `https://staging.codebuddy.cn`，用于切换 CodeBuddy CN 到预发环境；已读取目标片段确认修改生效，`read_lints` 无诊断。
