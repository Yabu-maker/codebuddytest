# 2026-05-27

- `report.html`：把 `<h1>` 中静态的 `🌍` 改造为动态地球图标。新增 `.globe` span，通过 `globe-spin`（左右摆动+缩放）、`globe-float`（上下浮动）以及 `::before` 的 `globe-rotate-emoji`（在 🌍/🌎/🌏 之间循环）三组动画叠加实现自转感，并加蓝色 `drop-shadow`。
- 在 macOS 提醒事项中通过 AppleScript 创建了 5/27 18:00 的"遛狗"待办（Reminder ID: 9020F544-5D7A-43A0-B5E8-5F779CB5CAB9）。注意：macOS 日历不支持"待办"，需用 Reminders App；首次脚本失败是因为 Reminders 进程未启动，先 `open -a Reminders` 再执行即可。
- `report.html`：在 `</body>` 前追加「附录：12 种常见编程语言全景」章节。①引入 Chart.js 4.4.1 + highlight.js 11.9.0（CDN）；②内置 12 种语言（Python/JS/TS/Java/C#/C++/Go/Rust/R/SQL/Kotlin/Swift）的 Pearson 相关系数等价代码片段，含一键复制+标签页切换+语法高亮；③4 个 Chart.js 图表（TIOBE 横向条形、应用场景 doughnut、GitHub Stars 折线趋势、6 语言特征雷达）；④动效：CSS 打字机标题、星点漂移背景、计数器入场、bar 进度条 IntersectionObserver 充能、悬浮 mini 卡片、终端光标闪烁；⑤含 12 行综合特征对比表。代码片段通过 escapeHtml 转义防 XSS；SQL 示例使用纯聚合避免注入。
- 新增 `mermaid_showcase.md`（980 行，30 个 Mermaid 代码块，19 种图类型）。
- 新增 `yaml_frontmatter_showcase.md`（1213 行，25 个 YAML 代码块，16 个场景）。覆盖：YAML 全数据类型速览（字符串/数字/布尔/日期/列表/映射/锚点/别名）、Jekyll/Hugo/Hexo 博客文章、MkDocs Material/VuePress/Docusaurus 文档、Slidev/Marp/reveal.js 幻灯片、Pandoc/Quarto 学术论文、Obsidian/Foam/Logseq 知识库笔记、mdBook/GitBook 电子书、API 文档、更新日志、多语言 i18n、电商商品页、个人简历、开源 README、数据分析报告、YAML 高级语法（锚点合并/显式类型标签/多行字符串全写法/特殊键名/数值边界/日期全格式），末附 6 大工具字段速查表。脚本程序化生成，覆盖：YAML Frontmatter、6 级标题、列表/任务/嵌套、引用与 GFM alert、表格（基础/对齐/复杂/长表 30 行）、围栏代码（30 种语言）、链接图片、转义/HTML 实体/Unicode/全语种 Hello/特殊空白、HTML 嵌入（details/kbd/mark/abbr）、KaTeX 数学公式（含 Maxwell 方程组、矩阵、分段函数）、脚注、定义列表、缩写、上下标高亮、Emoji 8 大类、16 种 Mermaid 图（flowchart/sequence/class/state/er/gantt/pie/journey/gitGraph/timeline/mindmap/quadrant/sankey/requirement/c4）、20.x 章节循环 280 个综合样本块。目录锚点使用 `to_anchor()` 函数（GitHub 标准：小写+空格转-+删除非字母数字字符）正确生成，修复了原先 `(#)` 空锚点导致目录不能跳转的问题。
